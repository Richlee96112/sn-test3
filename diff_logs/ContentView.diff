diff --git a/Views/ContentView.swift b/Views/ContentView.swift
--- a/Views/ContentView.swift
+++ b/Views/ContentView.swift
--- /Users/gitaeklee/Desktop/study/Snailer-Test2/Snailer-todo/diff_logs/ContentView.original	2025-08-07 20:02:18
+++ /Users/gitaeklee/Desktop/study/Snailer-Test2/Snailer-todo/diff_logs/ContentView.updated	2025-08-07 20:02:18
@@ -1,3 +1,55 @@
+import SwiftUI
+
+struct ContentView: View {
+  @StateObject private var viewModel = TaskViewModel()
+
+  var body: some View {
+    VStack {
+      Text("오늘의 할 일")
+        .font(.largeTitle)
+        .fontWeight(.bold)
+        .padding(.top)
+
+      List {
+        ForEach(viewModel.tasks) { task in
+          HStack {
+            Image(systemName: task.isCompleted ? "checkmark.circle.fill" : "circle")
+              .foregroundColor(task.isCompleted ? .green : .gray)
+
+            Text(task.title)
+              .strikethrough(task.isCompleted)
+              .foregroundColor(task.isCompleted ? .gray : .primary)
+          }
+          .contentShape(Rectangle())
+          .onTapGesture {
+            viewModel.toggleTask(task)
+          }
+        }
+        .onDelete { indexSet in
+          indexSet.forEach { index in
+            viewModel.deleteTask(viewModel.tasks[index])
+          }
+        }
+      }
+
+      HStack {
+        TextField("새로운 할 일", text: $viewModel.newTaskTitle)
+          .textFieldStyle(RoundedBorderTextFieldStyle())
+
+        Button(action: { viewModel.addTask() }) {
+          Text("Add")
+            .fontWeight(.bold)
+            .padding(.horizontal, 20)
+            .padding(.vertical, 10)
+            .background(Color.blue)
+            .foregroundColor(.white)
+            .cornerRadius(8)
+        }
+      }
+      .padding()
+    }
+  }
+}
 //
 //  ContentView.swift
 //  Snailer-todo
@@ -5,8 +57,6 @@
 //  Created by RichL on 7/28/25.
 //
 
-import SwiftUI
-
 struct ContentView: View {
   var body: some View {
     VStack {
