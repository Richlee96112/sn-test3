diff --git a/Utils/UserDefaultsManager.swift b/Utils/UserDefaultsManager.swift
--- a/Utils/UserDefaultsManager.swift
+++ b/Utils/UserDefaultsManager.swift
--- /Users/gitaeklee/Desktop/study/Snailer-Test2/Snailer-todo/diff_logs/UserDefaultsManager.original	2025-08-07 20:21:50
+++ /Users/gitaeklee/Desktop/study/Snailer-Test2/Snailer-todo/diff_logs/UserDefaultsManager.updated	2025-08-07 20:21:50
@@ -2,6 +2,7 @@
 
 class UserDefaultsManager {
   private let tasksKey = "tasks"
+  static let shared = UserDefaultsManager()
 
   func saveTasks(_ tasks: [Task]) {
     if let encoded = try? JSONEncoder().encode(tasks) {
@@ -18,3 +19,22 @@
     return tasks
   }
 }
+
+class UserDefaultsManager {
+  private let tasksKey = "tasks"
+
+  func saveTasks(_ tasks: [Task]) {
+    if let encoded = try? JSONEncoder().encode(tasks) {
+      UserDefaults.standard.set(encoded, forKey: tasksKey)
+    }
+  }
+
+  func loadTasks() -> [Task] {
+    guard let data = UserDefaults.standard.data(forKey: tasksKey),
+      let tasks = try? JSONDecoder().decode([Task].self, from: data)
+    else {
+      return []
+    }
+    return tasks
+  }
+}
